import React from 'react';
import ModalContent from '@components/Header/ModalContent';

interface IProps {}
const ShowAllProfileModal = ({}: IProps) => {
  return (
    <ModalContent title={'프로필 선택'}>
      <div>...</div>
    </ModalContent>
  );
};

export default ShowAllProfileModal;
